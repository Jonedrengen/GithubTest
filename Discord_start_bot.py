# Importing the custom Discord_bot module that contains the run_discord_bot function
import Discord_bot

# Ensuring the code below is only executed if this script is run directly (not imported as a module)
if __name__ == '__main__':
    # Calling the run_discord_bot function from the imported Discord_bot module to start the bot
    Discord_bot.run_discord_bot()